<!DOCTYPE html>
<html lang="pt-br">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css">
  <title>Lista de Produtos</title>
</head>
<body>
  <div class="container">
    <h1 class="text-center">Lista de Produtos Cadastrados</h1>

    <table class="table table-striped">
      <thead>
        <tr>
          <th scope="col">Cód</th>
          <th scope="col">cor</th>
          <th scope="col">Marca</th>
          <th scope="col">Quant</th>
          <th scope="col">Preço R$</th>
        </tr>
      </thead>
      <tbody>
        <% produtos.forEach(prod => { %>
          <tr>
            <th scope="row" class="text-center"><%=prod.id%></th>
            <td><%=prod.cor%></td>
            <td><%=prod.modelo%></td>
            <td><%=prod.marca%></td>
            <td class="text-center"><%=prod.qtsEstoque%></td>
            <td class="text-end"><%=(prod.preco).toLocaleString('pt-br', {minimumFractionDigits: 2})%>&ensp;</td>
          </tr>
        <% }) %>
      </tbody>
    </table>

  </div>
</body>
</html>